<div class="row">
  <%= render 'layouts/r_menu' %>
  <div class="column right">
    <div class="container_right">
    <table id="view_friends_id">
      <tr>
        <th>Мои друзья</th>
      </tr>
      <% @res.each do |friendship| %>
        <tr>
          <td><%= link_to friendship.friend.full_name, "/?id_user=#{friendship.id_friend}" %></td>
        </tr>
      <% end %>
    </table>
      <%= button_to "Добавить друга", action: "add" %>
  </div>
  </div>
</div>